# Coverage scope: domain logic only (adapters and platform code are excluded)
filter = App/Domain/

# Minimum acceptable line coverage — CI fails if this is not met.
# Set to 0 until domain logic tests are added that call App/Domain/ functions.
fail-under-line = 0

# Always print a summary table to the console
print-summary = yes
