# Coverage scope: domain logic only (Tasks and platform code are excluded)
filter = App/Domain/

# Minimum acceptable line coverage — CI fails if this is not met
fail-under-line = 80

# Always print a summary table to the console
print-summary = yes
